apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "receipt-backend.fullname" . }}-test-connection"
  labels:
    {{- include "receipt-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "receipt-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
